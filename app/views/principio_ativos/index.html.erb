<h1>Listing principio_ativos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @principio_ativos.each do |principio_ativo| %>
  <tr>
    <td><%= principio_ativo.nome %></td>
    <td><%= link_to 'Show', principio_ativo %></td>
    <td><%= link_to 'Edit', edit_principio_ativo_path(principio_ativo) %></td>
    <td><%= link_to 'Destroy', principio_ativo, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Principio ativo', new_principio_ativo_path %>
