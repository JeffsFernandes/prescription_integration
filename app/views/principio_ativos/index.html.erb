<h1>Listar Princípios Ativos</h1>

<table>
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @principio_ativos.each do |principio_ativo| %>
  <tr>
    <td><%= principio_ativo.nome %></td>
    <td><%= link_to 'Ver', principio_ativo %></td>
    <td><%= link_to 'Editar', edit_principio_ativo_path(principio_ativo) %></td>
    <td><%= link_to 'Remover', principio_ativo, confirm: 'Confirmar?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Novo Princípio ativo', new_principio_ativo_path %>
