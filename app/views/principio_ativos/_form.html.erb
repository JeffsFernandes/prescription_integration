<%= form_for(@principio_ativo) do |f| %>
  <% if @principio_ativo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@principio_ativo.errors.count, "error") %> prohibited this principio_ativo from being saved:</h2>

      <ul>
      <% @principio_ativo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
